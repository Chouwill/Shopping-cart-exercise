# 購物車功能邏輯說明

## 📚 目錄

1. [filter() 函式的工作原理](#filter-函式的工作原理)
2. [比較運算子：`!==` vs `==` 的差異](#比較運算子--vs--的差異)
3. [為什麼 `!==` 能篩出「不要的東西」？](#為什麼--能篩出不要的東西)
4. [Composables vs Pinia 的差異](#composables-vs-pinia-的差異)
5. [實際應用範例](#實際應用範例)

---

## filter() 函式的工作原理

### 基本概念

`filter()` 是 JavaScript 陣列的一個內建方法，它的作用是**建立一個新陣列**，其中只包含通過測試函式（callback）的元素。

### 語法結構

```javascript
const newArray = array.filter(callbackFunction);
```

### 運作流程（Step by Step）

1. **遍歷陣列**：`filter()` 會依序檢查陣列中的每個元素
2. **執行測試函式**：對每個元素執行你提供的 callback 函式
3. **判斷真假值**：
   - 如果 callback 回傳 `true` → 該元素會被**保留**在新陣列中
   - 如果 callback 回傳 `false` → 該元素會被**排除**，不會出現在新陣列中
4. **回傳新陣列**：不會修改原始陣列，而是回傳一個全新的陣列

### 視覺化理解

假設你有一個購物車陣列：

```javascript
const cartList = [
  { id: 1, name: "商品A", quantity: 2 },
  { id: 2, name: "商品B", quantity: 0 },
  { id: 3, name: "商品C", quantity: 1 },
];
```

當你執行：

```javascript
const filtered = cartList.filter((item) => item.quantity !== 0);
```

**內部運作過程**：

| 元素                   | 測試條件 `item.quantity !== 0` | 結果    | 是否保留 |
| ---------------------- | ------------------------------ | ------- | -------- |
| { id: 1, quantity: 2 } | `2 !== 0` → `true`             | ✅ 保留 | 是       |
| { id: 2, quantity: 0 } | `0 !== 0` → `false`            | ❌ 排除 | 否       |
| { id: 3, quantity: 1 } | `1 !== 0` → `true`             | ✅ 保留 | 是       |

**最終結果**：

```javascript
filtered = [
  { id: 1, name: "商品A", quantity: 2 },
  { id: 3, name: "商品C", quantity: 1 },
];
```

---

## 比較運算子：`!==` vs `==` 的差異

### 核心差異

JavaScript 有兩組比較運算子：

| 運算子 | 名稱                        | 特性                                 |
| ------ | --------------------------- | ------------------------------------ |
| `==`   | 寬鬆相等（Loose Equality）  | 會進行**型別轉換**後再比較           |
| `===`  | 嚴格相等（Strict Equality） | **不進行型別轉換**，型別和值都要相同 |
| `!=`   | 寬鬆不等                    | 會進行型別轉換後再比較               |
| `!==`  | 嚴格不等                    | 不進行型別轉換，型別或值不同即為不等 |

### 為什麼推薦使用 `===` 和 `!==`？

**`==` 的問題**：型別轉換可能會造成意外的結果

```javascript
// 使用 == 的狀況
5 == "5"; // true（字串 "5" 被轉成數字 5）
0 == false; // true（false 被轉成 0）
"" == false; // true（空字串和 false 都被轉成 0）
null == undefined; // true（特殊規則）

// 使用 === 的狀況
5 === "5"; // false（型別不同：number vs string）
0 === false; // false（型別不同：number vs boolean）
"" === false; // false（型別不同：string vs boolean）
null === undefined; // false（型別不同：null vs undefined）
```

### 在你的專案中的使用場景

#### 場景 1：檢查 ID 是否相同（第 29 行）

```javascript
const findItem = cartList.value.find((item) => item.id == value.id);
```

**觀察**：這裡使用 `==`，在某些情況下可能會有意外的結果。如果 `item.id` 是數字 `5`，而 `value.id` 是字串 `"5"`，兩者會被視為相等。

**建議**：改用 `===` 更安全

```javascript
const findItem = cartList.value.find((item) => item.id === value.id);
```

#### 場景 2：篩選掉 quantity 為 0 的項目（第 55 行）

```javascript
const reduceItem = cartList.value.filter((item) => item.quantity !== 0);
```

**為什麼用 `!==`？**

- 我們要排除 `quantity === 0` 的項目
- 使用 `!==` 確保型別和值都要不同才為 `true`
- 這樣可以避免 `null`、`undefined`、`""` 等值被誤判

---

## 為什麼 `!==` 能篩出「不要的東西」？

### 邏輯反轉的理解

`filter()` 函式的工作原理是：**保留回傳 `true` 的元素，排除回傳 `false` 的元素**。

當你寫：

```javascript
cartList.value.filter((item) => item.quantity !== 0);
```

**這句話的意思是**：

- 檢查條件：`item.quantity !== 0`
- 如果 `quantity` **不等於** `0` → 回傳 `true` → **保留**這個項目
- 如果 `quantity` **等於** `0` → 回傳 `false` → **排除**這個項目

### 正反邏輯對比

#### 方法 1：使用 `!==`（排除 0）

```javascript
// 保留 quantity 不等於 0 的項目（排除等於 0 的）
const result = cartList.filter((item) => item.quantity !== 0);
```

| quantity 值 | `quantity !== 0` 結果 | 是否保留 |
| ----------- | --------------------- | -------- |
| 2           | `true`                | ✅ 保留  |
| 1           | `true`                | ✅ 保留  |
| 0           | `false`               | ❌ 排除  |

#### 方法 2：使用 `===`（只保留非 0）

```javascript
// 保留 quantity 等於非 0 的項目（邏輯上可行，但語意較不直觀）
const result = cartList.filter((item) => item.quantity > 0);
```

| quantity 值 | `quantity > 0` 結果 | 是否保留 |
| ----------- | ------------------- | -------- |
| 2           | `true`              | ✅ 保留  |
| 1           | `true`              | ✅ 保留  |
| 0           | `false`             | ❌ 排除  |

**結論**：兩種寫法結果相同，但 `!== 0` 更直接表達「排除 0」的意圖。

### 在 reduceQuantity 中的應用

讓我們看看你專案中的完整邏輯：

```javascript
function reduceQuantity(item) {
  if (item.quantity <= 1) {
    item.quantity = 0; // 先將數量設為 0
    const reduceItem = cartList.value.filter((item) => item.quantity !== 0);
    // ↑ 這行會把所有 quantity 不等於 0 的項目保留，等於 0 的排除
    cartList.value = reduceItem; // 更新購物車列表
  } else {
    item.quantity--; // 如果數量 > 1，就直接減 1
  }
}
```

**流程解析**：

1. 當數量 ≤ 1 時，先將該項目的 `quantity` 設為 `0`
2. 使用 `filter()` 配合 `!== 0` 的條件，**過濾掉所有 quantity 為 0 的項目**
3. 將過濾後的陣列重新賦值給 `cartList.value`

---

## Composables vs Pinia 的差異

### 1. Composables（你目前使用的方式）

#### 定義

Composables 是 Vue 3 提供的**可重用邏輯組織方式**，透過函式封裝狀態和邏輯，可以在多個組件中共享。

#### 特點

- ✅ **輕量級**：不需要額外安裝套件
- ✅ **靈活**：可以根據需求自由組合
- ✅ **簡單專案適用**：小型到中型專案的首選
- ⚠️ **狀態隔離**：每次呼叫 `useCart()` 都會建立**新的狀態實例**
- ⚠️ **無法跨組件共享狀態**：不同組件呼叫 `useCart()` 會得到不同的 `cartList`

#### 程式碼結構

```javascript
// src/composables/useCart.js
import { ref, computed } from "vue";

export const useCart = () => {
  const cartList = ref([]); // 每次呼叫都建立新的狀態

  function addCart(value) {
    // 邏輯...
  }

  return { cartList, addCart };
};
```

#### 使用方式

```javascript
// 在組件中使用
const { cartList, addCart } = useCart();
```

**⚠️ 重要限制**：如果兩個組件都呼叫 `useCart()`，它們會有**各自獨立的購物車狀態**，不會共享。

---

### 2. Pinia（狀態管理方案）

#### 定義

Pinia 是 Vue 3 的**官方狀態管理庫**，專門用於在**多個組件之間共享狀態**。

#### 特點

- ✅ **全域狀態共享**：所有組件共用同一個 store 實例
- ✅ **DevTools 支援**：可以在 Vue DevTools 中追蹤狀態變化
- ✅ **時間旅行除錯**：可以回溯狀態歷史
- ✅ **適合中大型專案**：當狀態需要在多個組件間共享時
- ⚠️ **需要額外安裝**：`npm install pinia`
- ⚠️ **較重**：對小型專案可能過度設計

#### 程式碼結構

```javascript
// src/store/cart.js
import { defineStore } from "pinia";
import { ref, computed } from "vue";

export const useCartStore = defineStore("cartItem", () => {
  const cartList = ref([]); // 全域共享的狀態

  function addCart(value) {
    // 邏輯...
  }

  return { cartList, addCart };
});
```

#### 使用方式

```javascript
// 在組件中使用
import { useCartStore } from "@/store/cart";
const cartStore = useCartStore(); // 取得同一個 store 實例
```

**✅ 優勢**：無論在哪個組件中呼叫 `useCartStore()`，都會取得**同一個購物車狀態**。

---

### 3. 差異對照表

| 特性              | Composables                       | Pinia                       |
| ----------------- | --------------------------------- | --------------------------- |
| **狀態共享**      | ❌ 每次呼叫都是新實例             | ✅ 全域共享同一個實例       |
| **適用規模**      | 小型到中型專案                    | 中大型專案                  |
| **學習曲線**      | 較低（就是 Vue 3 的 composables） | 中等（需要理解 store 概念） |
| **DevTools 支援** | ❌ 無                             | ✅ 完整支援                 |
| **套件依賴**      | 無（Vue 3 內建）                  | 需要安裝 `pinia`            |
| **使用場景**      | 單一組件或少量組件共享邏輯        | 多個組件需要共享狀態        |

### 4. ⚠️ 重要：語法看起來一樣，但行為完全不同！

#### 語法對比

**Composables：**

```javascript
// src/composables/useCart.js
export const useCart = () => {
  const cartList = ref([]);
  function addCart(value) { ... }
  return { cartList, addCart };
};
```

**Pinia：**

```javascript
// src/store/cart.js
export const useCartStore = defineStore("cartItem", () => {
  const cartList = ref([]);
  function addCart(value) { ... }
  return { cartList, addCart };
});
```

**你發現了嗎？** 除了 `defineStore("cartItem", ...)` 這部分，語法幾乎一模一樣！

#### 關鍵差異：實例的建立方式

|                    | Composables               | Pinia                                      |
| ------------------ | ------------------------- | ------------------------------------------ |
| **語法**           | `useCart = () => { ... }` | `defineStore("name", () => { ... })`       |
| **每次呼叫時**     | 執行函式，建立**新實例**  | Pinia 檢查是否已有實例，有就回傳**同一個** |
| **記憶體中的狀態** | 每個組件各自獨立          | 所有組件共用同一個                         |

#### 實際範例：狀態共享差異

**使用 Composables（目前專案）**

```javascript
// 組件 A
const { cartList, addCart } = useCart();
addCart({ id: 1, name: "商品A" });
console.log(cartList.value); // [{ id: 1, name: "商品A" }]

// 組件 B（不同的實例）
const { cartList, addCart } = useCart();
console.log(cartList.value); // [] ← 空的！因為是新實例
```

**使用 Pinia**

```javascript
// 組件 A
const cartStore = useCartStore();
cartStore.addCart({ id: 1, name: "商品A" });
console.log(cartStore.cartList); // [{ id: 1, name: "商品A" }]

// 組件 B（同一個 store 實例）
const cartStore = useCartStore();
console.log(cartStore.cartList); // [{ id: 1, name: "商品A" }] ← 共享狀態！
```

#### 🔬 如何驗證差異？

我已經為你建立了一個示範組件 `ComposablesVsPiniaDemo.vue`，你可以：

1. **在 App.vue 中引入示範組件**：

```vue
<script setup>
import ComposablesVsPiniaDemo from "./components/ComposablesVsPiniaDemo.vue";
</script>

<template>
  <ComposablesVsPiniaDemo />
</template>
```

2. **實際操作驗證**：

   - 點擊「組件 A」的按鈕加入商品 → 觀察組件 A 的購物車
   - 點擊「組件 B」的按鈕加入商品 → 觀察組件 B 的購物車
   - **你會發現：組件 A 和 B 的購物車是分開的！**（Composables）

   - 點擊「組件 C」的按鈕加入商品 → 觀察組件 C 和 D 的購物車
   - 點擊「組件 D」的按鈕加入商品 → 觀察組件 C 和 D 的購物車
   - **你會發現：組件 C 和 D 的購物車是共享的！**（Pinia）

3. **在 Console 中驗證**：

```javascript
// 在瀏覽器 Console 中執行

// Composables：每次都是新實例
const { cartList: cart1 } = useCart();
const { cartList: cart2 } = useCart();
console.log(cart1 === cart2); // false ← 不同實例！

// Pinia：都是同一個實例
const store1 = useCartStore();
const store2 = useCartStore();
console.log(store1 === store2); // true ← 同一個實例！
console.log(store1.cartList === store2.cartList); // true ← 同一個狀態！
```

---

## 實際應用範例

### 範例 1：filter() 的其他常見用法

```javascript
// 篩選價格大於 100 的商品
const expensiveItems = products.filter((item) => item.price > 100);

// 篩選有庫存的商品（庫存 > 0）
const inStockItems = products.filter((item) => item.stock > 0);

// 篩選特定類別的商品
const electronics = products.filter((item) => item.category === "electronics");

// 篩選名稱包含關鍵字的商品
const searchResults = products.filter((item) =>
  item.name.toLowerCase().includes("手機".toLowerCase())
);
```

### 範例 2：為什麼要用 `!==` 而不是 `==`

```javascript
// ❌ 不建議：使用 == 可能造成意外結果
const items = [
  { id: 1, quantity: 0 },
  { id: 2, quantity: "0" }, // 字串 "0"
  { id: 3, quantity: null },
  { id: 4, quantity: 2 },
];

// 使用 ==
const filtered1 = items.filter((item) => item.quantity != 0);
// 結果：[{ id: 2, quantity: "0" }] ← 意外！"0" == 0 為 true，被排除了
// 但 { id: 3, quantity: null } 也被排除了（null == 0 在某些情況可能意外）

// ✅ 建議：使用 !==
const filtered2 = items.filter((item) => item.quantity !== 0);
// 結果：[{ id: 2, quantity: "0" }, { id: 3, quantity: null }, { id: 4, quantity: 2 }]
// 更精確：只有真正的數字 0 會被排除
```

### 範例 3：在你的購物車中的完整流程

```javascript
// 初始購物車
cartList.value = [
  { id: 1, name: "商品A", quantity: 2 },
  { id: 2, name: "商品B", quantity: 1 },
  { id: 3, name: "商品C", quantity: 1 },
];

// 使用者點擊減少商品B的數量
reduceQuantity({ id: 2, name: "商品B", quantity: 1 });

// 執行流程：
// 1. quantity (1) <= 1，進入 if 區塊
// 2. item.quantity = 0（將商品B的數量設為0）
// 3. cartList.value 現在是：
//    [
//      { id: 1, name: "商品A", quantity: 2 },
//      { id: 2, name: "商品B", quantity: 0 }, ← 變成 0
//      { id: 3, name: "商品C", quantity: 1 }
//    ]
// 4. filter((item) => item.quantity !== 0) 執行：
//    - id: 1, quantity: 2 → 2 !== 0 → true → 保留 ✅
//    - id: 2, quantity: 0 → 0 !== 0 → false → 排除 ❌
//    - id: 3, quantity: 1 → 1 !== 0 → true → 保留 ✅
// 5. reduceItem = [
//      { id: 1, name: "商品A", quantity: 2 },
//      { id: 3, name: "商品C", quantity: 1 }
//    ]
// 6. cartList.value = reduceItem（更新購物車）

// 最終結果：商品B被移除了
```

---

## 📖 官方文件連結

### JavaScript 陣列方法

- [MDN: Array.prototype.filter()](https://developer.mozilla.org/zh-TW/docs/Web/JavaScript/Reference/Global_Objects/Array/filter)
- [MDN: 比較運算子](https://developer.mozilla.org/zh-TW/docs/Web/JavaScript/Guide/Expressions_and_Operators#%E6%AF%94%E8%BC%83%E9%81%8B%E7%AE%97%E5%AD%90)

### Vue 3 Composables

- [Vue 3 官方文件：Composables](https://vuejs.org/guide/reusability/composables.html)
- [Vue 3 官方文件：組合式函式（Composition API）](https://vuejs.org/guide/extras/composition-api-faq.html)

### Pinia

- [Pinia 官方文件](https://pinia.vuejs.org/)
- [Pinia 快速開始](https://pinia.vuejs.org/getting-started.html)
- [Pinia vs Vuex 比較](https://pinia.vuejs.org/introduction.html#comparison-with-vuex)

---

## 🎯 總結

1. **`filter()`**：用於篩選陣列，保留回傳 `true` 的元素，排除回傳 `false` 的元素
2. **`!==`**：嚴格不等比較，不會進行型別轉換，比 `==` 更安全可靠
3. **`!== 0` 的作用**：排除 quantity 為 0 的項目，保留所有非 0 的項目
4. **Composables**：適合單一或少數組件共享邏輯，每次呼叫都是新實例
5. **Pinia**：適合多組件共享狀態，全域共用同一個 store 實例

---

## ✅ 檢查清單

- [ ] 理解 `filter()` 如何透過回傳 `true/false` 來篩選元素
- [ ] 理解 `!==` 與 `==` 的差異（型別轉換 vs 嚴格比較）
- [ ] 理解為什麼 `item.quantity !== 0` 能排除 quantity 為 0 的項目
- [ ] 理解 Composables 與 Pinia 的語法相似但行為不同
- [ ] 理解 Composables 與 Pinia 的使用場景差異
- [ ] 知道何時該選擇 Composables，何時該選擇 Pinia

---

## 🧪 實際演練：測試 Composables vs Pinia 的差異

### 步驟 1：切換到示範頁面

暫時修改 `src/App.vue`，引入示範組件：

```vue
<script setup>
// 暫時註解掉原本的購物車邏輯
// import { useCart } from "./composables/useCart";
import ComposablesVsPiniaDemo from "./components/ComposablesVsPiniaDemo.vue";
</script>

<template>
  <ComposablesVsPiniaDemo />
</template>
```

### 步驟 2：執行並觀察

1. **啟動開發伺服器**（如果還沒啟動）：

   ```bash
   npm run dev
   ```

2. **測試 Composables（藍色區塊）**：

   - 點擊「組件 A」的「加入商品 A 到購物車」按鈕
   - 觀察組件 A 的購物車：應該會顯示商品 A
   - 點擊「組件 B」的「加入商品 B 到購物車」按鈕
   - 觀察組件 B 的購物車：應該會顯示商品 B
   - **關鍵觀察**：組件 A 的購物車**不會**出現商品 B，組件 B 的購物車**不會**出現商品 A
   - **結論**：兩個組件使用**各自獨立的購物車狀態**

3. **測試 Pinia（紫色區塊）**：
   - 點擊「組件 C」的「加入商品 C 到購物車」按鈕
   - 觀察組件 C 和組件 D 的購物車：**兩者都會顯示商品 C**
   - 點擊「組件 D」的「加入商品 D 到購物車」按鈕
   - 觀察組件 C 和組件 D 的購物車：**兩者都會顯示商品 C 和商品 D**
   - **關鍵觀察**：無論在哪個組件操作，兩個組件的購物車內容**完全相同**
   - **結論**：兩個組件使用**同一個共享的購物車狀態**

### 步驟 3：在 Console 中驗證（進階）

打開瀏覽器的開發者工具（F12），在 Console 中執行：

```javascript
// 注意：這些測試需要在組件中使用 import 才能執行
// 以下是概念驗證

// Composables 測試
// 每次呼叫 useCart() 都會建立新的實例
const instance1 = useCart();
const instance2 = useCart();
console.log(
  "Composables 實例相同嗎？",
  instance1.cartList === instance2.cartList
);
// 預期結果：false（不同實例）

// Pinia 測試
// 每次呼叫 useCartStore() 都會取得同一個實例
const store1 = useCartStore();
const store2 = useCartStore();
console.log("Pinia 實例相同嗎？", store1 === store2);
// 預期結果：true（同一個實例）
console.log("Pinia 狀態相同嗎？", store1.cartList === store2.cartList);
// 預期結果：true（同一個狀態）
```

### 步驟 4：還原到原本的購物車

測試完畢後，記得將 `App.vue` 還原：

```vue
<script setup>
import { useCart } from "./composables/useCart";
// import ComposablesVsPiniaDemo from "./components/ComposablesVsPiniaDemo.vue";

const {
  products,
  cartList,
  cartTotal,
  addCart,
  addQuantity,
  reduceQuantity,
  cartAllTotal,
} = useCart();
</script>

<template>
  <!-- 原本的購物車模板 -->
</template>
```

---

## 💡 關鍵理解：為什麼語法相似但行為不同？

### 底層機制差異

**Composables：**

```javascript
// 每次呼叫都是一個新的函式執行
export const useCart = () => {
  const cartList = ref([]); // ← 每次執行都會建立新的 ref
  return { cartList };
};

// 組件 A 中
const { cartList: cartA } = useCart(); // 執行函式，建立新實例

// 組件 B 中
const { cartList: cartB } = useCart(); // 再次執行函式，建立另一個新實例
// cartA !== cartB（不同實例）
```

**Pinia：**

```javascript
// defineStore 會註冊一個 store 定義
export const useCartStore = defineStore("cartItem", () => {
  const cartList = ref([]); // ← 只會在第一次呼叫時建立
  return { cartList };
});

// 組件 A 中
const storeA = useCartStore(); // Pinia 檢查：已有 "cartItem" 實例嗎？沒有，建立一個

// 組件 B 中
const storeB = useCartStore(); // Pinia 檢查：已有 "cartItem" 實例嗎？有！回傳同一個
// storeA === storeB（同一個實例）
```

### 記憶體中的狀態

```
Composables（每次都是新實例）：
┌─────────────┐
│  組件 A     │
│  cartList   │──┐
└─────────────┘  │ 不同的記憶體位置
                 │
┌─────────────┐  │
│  組件 B     │  │
│  cartList   │──┘
└─────────────┘

Pinia（共用同一個實例）：
┌─────────────┐
│  組件 C     │
│  store      │──┐
└─────────────┘  │
                 ├──→ 同一個 cartList（同一塊記憶體）
┌─────────────┐  │
│  組件 D     │  │
│  store      │──┘
└─────────────┘
```

---

## 🎓 學習重點回顧

1. **語法相似性**：Pinia 的 Setup Store 語法確實和 Composables 非常相似，這是 Pinia 的設計巧思
2. **行為差異**：關鍵在於 Pinia 的 `defineStore` 會**快取實例**，確保全域共享
3. **選擇依據**：
   - 單一組件或少量組件 → Composables
   - 多個組件需要共享狀態 → Pinia
4. **實際驗證**：透過示範組件可以清楚看到差異

---

## ⚠️ 重要：持續呼叫新實例會怎樣？

### 問題情境

如果持續呼叫 `useCart()` 建立新實例，會發生什麼事？

### 影響分析

#### 1. 記憶體消耗

每次呼叫 `useCart()` 都會建立：

- 新的 `ref` 實例（`cartList`、`cartTotal`、`products`）
- 新的 `computed` 實例（`cartAllTotal`）
- 新的函式實例（`addCart`、`addQuantity`、`reduceQuantity`）
- 可能還有額外的響應式追蹤系統

**記憶體消耗計算**：

```
單一實例記憶體 ≈ 幾 KB
10 個實例 ≈ 幾十 KB
100 個實例 ≈ 幾百 KB
1000 個實例 ≈ 幾 MB
```

#### 2. 效能影響

- **記憶體使用增加**：每個實例都會佔用記憶體
- **垃圾回收壓力**：如果實例沒有被正確釋放，會增加垃圾回收的負擔
- **可能的記憶體洩漏**：如果實例被保留在陣列或全域變數中，不會被自動回收

#### 3. 實際場景範例

**❌ 錯誤範例：在迴圈中建立多個實例**

```javascript
// 危險！會建立 100 個獨立實例
for (let i = 0; i < 100; i++) {
  const { cartList } = useCart();
  // 每個實例都佔用記憶體
}
```

**❌ 錯誤範例：在事件處理中重複呼叫**

```javascript
// 危險！每次點擊都建立新實例
function handleClick() {
  const { cartList } = useCart(); // 每次點擊都建立新實例
  cartList.value.push(item);
}
```

**✅ 正確範例：只在 setup 中呼叫一次**

```javascript
// 正確：只建立一個實例
const { cartList, addCart } = useCart();

function handleClick() {
  addCart(item); // 使用同一個實例
}
```

### Vue 的垃圾回收機制

#### Composables 實例何時會被回收？

1. **組件卸載時**：當組件被銷毀（unmounted）時，Vue 會清理組件內的所有響應式狀態
2. **沒有其他引用時**：如果沒有其他地方引用這個實例，JavaScript 的垃圾回收器會自動回收

**但要注意**：

- 如果實例被保存在全域變數、陣列或物件中，**不會被自動回收**
- 如果在迴圈中建立實例並保存，會造成記憶體洩漏

#### Pinia 的優勢

```javascript
// Pinia：無論呼叫多少次，永遠只有一個實例
for (let i = 0; i < 1000; i++) {
  const store = useCartStore(); // 永遠只有一個實例
  // 記憶體使用穩定，不會累積
}
```

### 實際測試：記憶體示範

我已經建立了一個**記憶體測試示範組件**（`MemoryLeakDemo.vue`），你可以：

1. **點擊右上角的「記憶體測試」按鈕**
2. **測試 Composables**：
   - 點擊「建立新實例」按鈕多次
   - 觀察實例數量會持續增加
   - 每個實例都佔用記憶體
3. **測試 Pinia**：
   - 點擊「呼叫 useCartStore()」按鈕多次
   - 觀察：無論呼叫多少次，實際實例數量永遠是 1
   - 記憶體使用穩定

### 對比表格

| 情境               | Composables                      | Pinia                      |
| ------------------ | -------------------------------- | -------------------------- |
| **呼叫 10 次**     | 建立 10 個實例（記憶體 10x）     | 只有 1 個實例（記憶體 1x） |
| **在迴圈中呼叫**   | ⚠️ 危險！會建立大量實例          | ✅ 安全，永遠只有一個      |
| **組件卸載時**     | 實例會被回收（如果沒有其他引用） | 實例保留（這是預期行為）   |
| **記憶體洩漏風險** | ⚠️ 高（如果錯誤使用）            | ✅ 低（不會建立多個實例）  |

### 💡 實際建議

#### Composables 正確使用方式

1. **只在組件的 `setup` 中呼叫一次**

   ```javascript
   // ✅ 正確
   const { cartList, addCart } = useCart();
   ```

2. **不要在迴圈中呼叫**

   ```javascript
   // ❌ 錯誤
   for (let i = 0; i < 100; i++) {
     const { cartList } = useCart();
   }
   ```

3. **不要在事件處理中重複呼叫**

   ```javascript
   // ❌ 錯誤
   function handleClick() {
     const { cartList } = useCart();
   }

   // ✅ 正確
   const { cartList } = useCart();
   function handleClick() {
     // 使用 cartList
   }
   ```

#### Pinia 使用方式

- **可以在任何地方安全地呼叫**：不會建立多個實例
- **適合需要全域共享狀態的場景**：多個組件需要共用同一個狀態

### 🎯 總結

1. **Composables**：

   - 每次呼叫都建立新實例
   - 如果錯誤使用（如在迴圈中呼叫），會造成記憶體消耗
   - 正確使用時（只在 setup 中呼叫一次），不會有問題

2. **Pinia**：

   - 永遠只有一個實例
   - 不會有記憶體累積的問題
   - 適合需要全域共享狀態的場景

3. **關鍵原則**：
   - Composables：**一個組件，一次呼叫**
   - Pinia：**隨時呼叫，永遠只有一個**
